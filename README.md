# SIMP

Vite static website prepared for GitHub and Vercel deployment.

## Development

```bash
npm install
npm run dev
```

## Production Build

```bash
npm run build
```

Vercel can deploy this repository from the root directory using the default Vite settings:

- Build command: `npm run build`
- Output directory: `dist`
